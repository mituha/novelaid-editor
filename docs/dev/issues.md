作業予定、未解決の問題等のメモ
================================

## TODO

メタデータ用の右ペインの表示をマークダウンと小説の対応時のみとする。

### 起動時遅い

ファイルランチャーでプロジェクトを選択時、メタデータの読み込み処理等が行われます。
それらが終了するまで待っているのか、状態が変わらない状態が続きます。
なんらかのメッセージや途中経過表示をするか、動作自体を見直した方が良い。

### マークダウン表示

マークダウンのプレビューでリンクをクリックするとエラーになる。
表示領域自体がおかしくなる。

マークダウンのドキュメントでリンクをクリックできるようにしてファイルの切り替えを行いたい。

### 閲覧モードの追加

プレビュー同様の画面を別ドキュメントではなく表示する。
エディター画面内で編集モードと閲覧モードを切り替えられるようにする

### git未インストール時の処理？

メアド、名前の登録状況等をチェックした方が良い？

### 半角空白等の可視化

Monaco Editor の機能だが、動作していない？

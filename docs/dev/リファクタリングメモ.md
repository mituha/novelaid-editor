# リファクタリングメモ

バイブコーディングで書かせているため、あちこち問題がある場合の修正用のメモ。

## React.FC を使用せず、通常の関数の形で書きます



## 左右ペイン部分

LeftPane、RightPaneにpropsで渡しているのは内部で扱うパネルに依存している。
そのため、SidePaneのみとすることが出来ていない。
渡している内容について順に確認、調整していきます。
まずは、LeftPaneのonFileSelectから検討。
これに関してはドキュメントを開くだけのため、既に直接処理できるのでは？



## ファイルパスの扱いの再確認

documentsのキーとなっているパスの扱いなど。
ある程度調整したが、まだ各所に処理が散らばっており、きちんと処理されていない。
現状、内部的には絶対パスで処理されている。
結局、これはこれで良いかも。

## エディター部分

## テーマ切り替え部分のリファクタリング
現在、App.css等で扱われている部分を
`theme.css`、に分離し、切り替えや管理を、`ThemeContext.tsx`で行うように独立性を高めて下さい




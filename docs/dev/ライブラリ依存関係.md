# ライブラリ依存関係

ライブラリの依存関係で動作しなくなったこともあり、ライブラリの依存関係の確認方法等を記載します。

```pwsh
# 基本
npm list
# npmやnodeのチェック
npm doctor
# npm doctor でバージョンが低かった場合
# 最新の安定版に更新
npm install -g npm@latest
# 特定のバージョンを指定する場合
npm install -g npm@10.2.4

# nodeのバージョン更新用
nvm -v

```
nvmが未インストールの場合、インストール
https://github.com/coreybutler/nvm-windows/releases/latest

```pwsh
# nodeのバージョン確認
nvm list
# LTS
nvm install lts
nvm use lts
# 最新版
nvm install latest
nvm use latest
```


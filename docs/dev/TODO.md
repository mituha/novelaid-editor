TODO
=========================


## マークダウン表示

マークダウン表示時、コードブロックの内容などを独自の形式で処理できるような構造を追加します。
現在、カクヨム記法のルビ表示に対応しているが、どこでどのように処理しているかを確認。
これらの処理を共通化し、拡張しやすい構造にします。
これは、将来的にプラグインなどでの機能拡張を容易にするためです。

まず、ObsidianやVSCodeのMarkdown Preview Enhancedの拡張機能のように、marmeid.jsのコードブロックで図が表示できる機能の追加を検討します。

なお、コードは`src/novelaid-markdown/`以下に実装します。

## マークダウンのコードブロックでの地図表示

`react-map-gl`を使用して、コードブロックで地図を表示できるようにします。  
なお、コードブロックの識別名は、`carto-paw`,`map`,`leaflet`等を認識します。 
`carto-pow`は別途作成している地図作成ツールです。
`leaflet`は現在Obsidianで使用しているプラグインの互換用です。  

コードブロックの中身としては、まず、以下のような項目を認識させます。

```carto-paw
height: 500px
width: 500px
lat: -45.03531642894818 #初期表示緯度
long: 168.6620208020021 #初期表示経度
scale: 1
marker: #マーカー
  - default, -41.25745734170178, 174.86551771469374, M29PAF
  - default, -41,+174, M29PAE
  - default, -46.19769082957884, 169.7900927941849,トゥアタヒ村(辺境の地)M2EPA9
  - default,-45.03531642894818, 168.66202080200216,ムトゥンガ遺跡(クイーンズタウン)
  - default,-43.50619093881692, 172.67310773830928,首都ヌイパパ(クライストチャーチ)
  - default,-45.862833281549776, 170.52993506740208,トゥアルアの街(ダニーデン)
```

形式はYAMLを使用してObsidianのプラグインの互換性をもった最低限の情報のみ対応します。
まず、lat,longのみ対応するところから始めます。






## WebContentsView のよる表示

現在のURL表示の問題点として、複数のbrowserタブの切替時にリセットされます。  
内部的な非表示で回避することは可能かもしれませんが、根本的な解決にはなりません。
そこで、WebContentsView を使用するように変更します。

## 自動更新機能の改善

現状は標準？的な動作の実装のままとなっています。
`electron-updater`の機能？
この更新タイミング等がわかりにくいため、手動での更新メニューの追加や更新確認ボタン、更新機能等が必要。
最初のプロジェクト選択画面での更新チェックや必要に応じた更新ボタンがあれば良い？


## ファイルエクスプローラー関連

* 新規ファイル作成時、作成したファイルを開く
* 開いているツリーを閉じる機能
* 現在選択しているドキュメントとの同期
  + 基本的には無条件に同期で良い？
  + 設定の深いところと切り替わると面倒かもしれないから同期ボタンで開けば良いかも

### ファイルエクスプローラーに開いているドキュメントの表示

ファイルエクスプローラーに現在開いているドキュメントのリスト表示を追加。
現在のファイル一覧表示の上に閉じた形で展開できるツリー表示とする。

`.gitignore`や`.novelaidattributes`を表示する場合の対応。
暫定的には`plaintext`にすれば良い？
MonacoEditorのlanguageとしては、iniやshellもありそう。
現状、そのまま渡せる名前にしているが、変換部が有った方が良いかも。

非対応なバイナリはどうする？
pdfとかは？

JSONファイルはドキュメントタイプとして対応しておく。
アイコンは`file-braces-corner`あたり？

## ドキュメントエリア

### ドキュメントのタブの表示

タブの表示部分が多くなり過ぎた場合用に左右にスクロールボタンを追加。
タブ部分にコンテキストメニューで
* 閉じる
* 全て閉じる
* 右/左にも表示/分割
  + 分割ボタンと同じ役割になる
* 右/左側の分割領域に移動
  + 反対側の領域に同一ドキュメントがない場合
のような意味合いのメニューを追加する。
他にも必要なものがあれば追加

タブ部分にもドキュメントのアイコンを追加する。

## エディター部分

タブ切替時のカーソル、スクロール位置の保持？


## AI関連

thinkのON/OFF機能。
通常はOFFで良い。
単なるチャット用だと遅い

AI設定画面の調整。
接続テストが欲しいかも？

使用トークン数等の表示追加

### ツール機能への対応
現在、一部作業中。
AIチャット等でAI側がファイルの検索や中身の閲覧を可能にする。
これにより、コンテキストに含まれない範囲や参照する内容をAIが取得できるようにする。
将来的にAIエージェントによる編集作業も可能にする。



### AIコンテキスト選択をAI校正パネルにも追加

### チャットビューとAIチャットパネルの部品の共通化

AIチャットパネルも内部的にドキュメントを取る形にすれば履歴を保持した状態でのチャットが可能になる。
ドキュメントは`.novelaid`フォルダーに保存する。

## メタデータ関連

メタデータのプロパティ編集部に編集中のファイル名表示が有った方が良い。

### メタデータリスト表示

登場人物一覧や地名一覧部分。
拡張子は表示せずにスッキリさせる。
タイトル表示。
//TODO タイトル表示仕様を決めた方が良い


## 文章校正関連(非AI)

一文に二回表示されている助詞とかのチェックが入るが、波線は一つしか引かれない。
textlintの表示位置の扱いを調査する。複数位置の指摘に対応していた？


## 検索

ファイルの中身だけでなくファイル名からの検索も対応する。
同時に検索すれば良い。
表示方法は検討？
メタデータとか別の検索にも対応。
検索オプション、もしくは、適当にあいまいな検索もサポート


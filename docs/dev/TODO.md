TODO
=========================


## ドキュメントタイプの追加

現状、サポートしていないドキュメントに対する対応。
間違って読み書きしないようにする必要がある。

後述

## ファイルエクスプローラー関連

* 新規ファイル作成時、作成したファイルを開く
* 開いているツリーを閉じる機能
* 現在選択しているドキュメントとの同期
  + 基本的には無条件に同期で良い？
  + 設定の深いところと切り替わると面倒かもしれないから同期ボタンで開けば良いかも

### ファイルエクスプローラーに開いているドキュメントの表示

ファイルエクスプローラーに現在開いているドキュメントのリスト表示を追加。
現在のファイル一覧表示の上に閉じた形で展開できるツリー表示とする。

`.gitignore`や`.novelaidattributes`を表示する場合の対応。
暫定的には`plaintext`にすれば良い？
MonacoEditorのlanguageとしては、iniやshellもありそう。
現状、そのまま渡せる名前にしているが、変換部が有った方が良いかも。

非対応なバイナリはどうする？
pdfとかは？

JSONファイルはドキュメントタイプとして対応しておく。
アイコンは`file-braces-corner`あたり？

## ドキュメントエリア

### ドキュメントのタブの表示

タブの表示部分が多くなり過ぎた場合用に左右にスクロールボタンを追加。
タブ部分にコンテキストメニューで
* 閉じる
* 全て閉じる
* 右/左にも表示/分割
  + 分割ボタンと同じ役割になる
* 右/左側の分割領域に移動
  + 反対側の領域に同一ドキュメントがない場合
のような意味合いのメニューを追加する。
他にも必要なものがあれば追加

タブ部分にもドキュメントのアイコンを追加する。

## エディター部分

タブ切替時のカーソル、スクロール位置の保持？

### novel編集時

前のエピソードや次のエピソードへの移動ボタン(移動用UI)を追加。
上下にあると良い？
前後のエピソードのタイトル表示
エピソード自体は順序よく並んでいる前提。





## AI関連

thinkのON/OFF機能。
通常はOFFで良い。
単なるチャット用だと遅い

AI設定画面の調整。
接続テストが欲しいかも？

使用トークン数等の表示追加

### AIコンテキスト選択をAI校正パネルにも追加

### チャットビューとAIチャットパネルの部品の共通化

AIチャットパネルも内部的にドキュメントを取る形にすれば履歴を保持した状態でのチャットが可能になる。
ドキュメントは`.novelaid`フォルダーに保存する。

## メタデータ関連

メタデータのプロパティ編集部に編集中のファイル名表示が有った方が良い。

### メタデータリスト表示

登場人物一覧や地名一覧部分。
拡張子は表示せずにスッキリさせる。
タイトル表示。
//TODO タイトル表示仕様を決めた方が良い


## 文章校正関連(非AI)

一文に二回表示されている助詞とかのチェックが入るが、波線は一つしか引かれない。
textlintの表示位置の扱いを調査する。複数位置の指摘に対応していた？


## 検索

ファイルの中身だけでなくファイル名からの検索も対応する。
同時に検索すれば良い。
表示方法は検討？
メタデータとか別の検索にも対応。
検索オプション、もしくは、適当にあいまいな検索もサポート

